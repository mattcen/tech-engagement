include(`replacement.m4')
github_repo
===========

talk_tagline

Note: this repository depends on Reveal.js, so after
cloning, be sure to do a 'git submodule init; git submodule update'
to grab that.

View this presentation at https://github_user.github.io/github_repo
